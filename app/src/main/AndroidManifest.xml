<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="org.tvbrowser.permission.BIND_PLUGIN" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".core.EnigmaBridgeApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EnigmaBridge"
        tools:targetApi="31">

        <!-- Activities -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.timer.TimerListActivity"
            android:label="@string/label_scheduled_timers" />

        <activity
            android:name=".ui.receiversettings.ReceiverSettingsActivity"
            android:label="ReceiverSettings"
            android:parentActivityName=".ui.main.MainActivity" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:label="@string/title_settings"
            android:parentActivityName=".ui.main.MainActivity" />

        <activity
            android:name=".ui.timer.EditTimerActivity"
            android:label="@string/title_edit_schedule"
            android:parentActivityName=".ui.timer.TimerListActivity" />

        <activity
            android:name=".tvbrowser.AdvancedScheduleActivity"
            android:excludeFromRecents="true"
            android:label="@string/title_advanced_schedule"
            android:taskAffinity=""
            android:theme="@style/Theme.EnigmaBridge.Dialog" />

        <activity
            android:name=".ui.about.AboutActivity"
            android:label="@string/title_about"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Services -->
        <service
            android:name=".tvbrowser.TvBrowserPluginService"
            android:exported="true"
            android:label="1"
            android:permission="org.tvbrowser.permission.BIND_PLUGIN">
            <intent-filter>
                <action android:name="org.tvbrowser.intent.action.PLUGIN" />
            </intent-filter>
        </service>

        <!-- Broadcast Receivers -->
        <receiver
            android:name=".broadcasts.TimerCheckReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".broadcasts.SyncTriggerReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="io.github.legandy.enigmabridge.intent.ACTION_TRIGGER_SYNC" />
            </intent-filter>
        </receiver>

        <!-- ** THE DEFINITIVE FIX: Disable the default WorkManager initializer ** -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="remove" />

    </application>

    <queries>
        <package android:name="org.tvbrowser.tvbrowser" />
    </queries>

</manifest>
