<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to connect to the Enigma2 receiver -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- This allows TV-Browser to bind to our plugin service -->
    <uses-permission android:name="org.tvbrowser.permission.BIND_PLUGIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EnigmaBridge"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Activity 1: The user-facing settings screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The core of the plugin, now with the corrected integer label -->
        <service
            android:name=".RecordService"
            android:exported="true"
            android:label="1"
            android:permission="org.tvbrowser.permission.BIND_PLUGIN">
            <!-- This is the corrected discovery action from the Share Plugin's manifest -->
            <intent-filter>
                <action android:name="org.tvbrowser.intent.action.PLUGIN" />
            </intent-filter>
        </service>

    </application>

    <!-- This block allows your app to check if TV-Browser is installed -->
    <queries>
        <package android:name="org.tvbrowser.tvbrowser" />
    </queries>

</manifest>

